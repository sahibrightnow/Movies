import React from 'react'
import TitleContainer from '../containers/Title_Container'

const ShowScreen = ({ navigation, route }) => (
  <TitleContainer route={route} navigation={navigation} />
)

export default ShowScreen